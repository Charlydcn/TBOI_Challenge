{% extends 'base.html.twig' %}

{% block title %}Versus{% endblock %}

{% block metaTag %}
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/css/challenge/show_challenge_versus.css')}}">
{% endblock %}

{% block body %}

    <a href="{{ path('new_versus', {'id': challenge.id})}}">Create new Versus</a>

    

    {% if versus|length > 0 %}
        <p>Current versus : </p>
        {% for v in versus %}
        <div class="versus href-element" data-href="{{ path('show_versus', {'id': v.id})}}">
            <ul>
                <li>Versus #{{ v.id }}</li>
                <li>Creator : {{ v.creator.username }}</li>
                <li>Starting : <span class="date" data-date="{{ v.startDate|date('d:m:Y H:i') }}">{{v.startDate|date('d:m:Y H:i')}}</span></li>
                <li>Ends in : <span class="date" data-date="{{ v.endDate|date('d:m:Y H:i') }}">{{v.endDate|date('d:m:Y H:i')}}</span></li>
                <li>Slots : {{ v.players|length }} / {{ v.slots }}</li>
            </ul>
            <img src="{{ asset('/img/misc/swords_versus.png')}}" alt="">
        </div>
        {% endfor %}

        {% else %}
        <p>No versus for this challenge yet</p>

    {% endif %}

    <div id="compteur"></div>

{% endblock %}

{% block javascripts %}
    {# <script src="{{ asset('/js/challenge/show.js')}}"></script> #}
{% endblock javascripts %}
